{
  "description": "teams-progress — cuối phiên, gửi tóm tắt các commit chưa báo lên Teams",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-end.sh\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
