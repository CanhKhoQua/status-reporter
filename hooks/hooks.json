{
  "description": "status-reporter — when work has settled, ask whether to post a summary of unreported commits to a chat channel",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/prompt-nudge.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
