{
  "description": "status-reporter — at the end of a session, post a summary of unreported commits to a chat channel",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-end.sh\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
